# 미건 임대 주택 현황 — 실운영 전환 설정 가이드

이 문서는 AI가 대신할 수 없는, 계정 인증이 필요한 부분만 모았습니다. 코드는 이미 다 만들어져 있으니 아래 순서대로 클릭만 하면 됩니다. 선택하신 옵션 기준으로 정리했습니다: **알림톡 없이 SMS만 / 은행 문자 자동연동 없이 엑셀 업로드로만 대사 / Cloudflare 미사용 / GitHub Pages 배포**.

---

## 1. Google Sheets + Apps Script (필수, 약 10분)

1. [sheets.google.com](https://sheets.google.com)에서 새 스프레드시트를 만들고 이름을 "미건 임대 주택 DB"로 바꿉니다.
2. 상단 메뉴 **확장 프로그램 → Apps Script**를 클릭합니다.
3. 기본으로 열려있는 `Code.gs`의 내용을 전부 지우고, 이 폴더의 [`Code.gs`](Code.gs) 내용 전체를 붙여넣습니다.
4. 저장(Ctrl+S) 후, 상단 함수 선택 드롭다운에서 `setupSheets`를 골라 ▶ 실행 버튼을 누릅니다.
   - 처음 실행 시 "권한 검토" 팝업이 뜨면 **본인 계정 선택 → 고급 → (프로젝트명)으로 이동 → 허용**을 누릅니다. (Google이 "안전하지 않은 앱"이라고 경고하는 것은 본인이 만든 개인 스크립트라서 뜨는 정상적인 절차입니다.)
5. 스프레드시트로 돌아오면 시트 6개(세대/청구/입금/은행업로드/수리/알림로그)가 생기고, 상단에 **"미건 관리"** 메뉴가 새로 생깁니다. 새로고침(F5)해서 메뉴가 보이는지 확인하세요.
6. **"세대" 시트**에 실제 세대 정보를 입력합니다. 엑셀로 미리 정리해두셨다면 그대로 복사해서 붙여넣으면 됩니다(열 이름만 맞추면 순서는 상관없습니다). 이 폴더의 [`미건_임대주택_데이터양식.csv`](미건_임대주택_데이터양식.csv)를 열어보면 열 구성을 확인할 수 있습니다.
7. Apps Script 에디터로 돌아가 함수 드롭다운에서 `installTriggers`를 골라 실행합니다. "트리거 6개 설치 완료" 알림이 뜨면 끝입니다.

이제부터 매월 1일 청구 생성, 매일 미납 판정·문자 발송·대표 보고, 매주 월요일 리포트가 자동으로 돌아갑니다.

### 대시보드용 웹앱 배포 (실시간 조회 화면 연결)
1. Apps Script 에디터 우측 상단 **배포 → 새 배포**를 클릭합니다.
2. 유형에서 톱니바퀴를 눌러 **"웹 앱"**을 선택합니다.
3. "실행 계정"은 **나**, "액세스 권한이 있는 사용자"는 **모든 사용자**로 설정 후 **배포**를 누릅니다.
4. 나오는 **웹 앱 URL**(`https://script.google.com/macros/s/.../exec` 형태)을 복사해둡니다 — 4번 항목(GitHub Pages)에서 씁니다.

### 스크립트 속성 설정 (비밀값 — 시트가 아닌 여기에 저장)
Apps Script 에디터 좌측 **프로젝트 설정(톱니바퀴) → 스크립트 속성 → 속성 추가**에서 아래 키들을 넣습니다. 지금 당장 모두 채우지 않아도 되고, 없는 값은 해당 기능만 자동으로 건너뜁니다.

| 속성 이름 | 값 | 용도 |
|---|---|---|
| `WEBAPP_TOKEN` | 아무 임의의 긴 문자열 (예: `mg-2026-9f3a7c`) | 대시보드가 웹앱을 호출할 때 쓰는 비밀 토큰. 아무나 데이터를 못 보게 막습니다. |
| `OWNER_PHONE` | 대표님 휴대폰 번호 (010-0000-0000) | 카카오 연동 실패 시 SMS 백업 발송용 |
| `SMS_SENDER` | 솔라피에 등록한 발신번호 | 문자 발신번호 |
| `SOLAPI_API_KEY` / `SOLAPI_API_SECRET` | 솔라피 발급 값 | 2번 항목 참고 |
| `KAKAO_CLIENT_ID` / `KAKAO_REFRESH_TOKEN` | 카카오 발급 값 | 3번 항목 참고 |
| `GITHUB_TOKEN` / `GITHUB_REPO` / `GITHUB_PAGES_HOST` | 4번 항목 참고 | 주간 리포트 커밋용 |

---

## 2. 솔라피 (SMS 발송, 약 15분)

알림톡 없이 SMS만 쓰기로 하셨으므로 사업자 등록 없이 개인 인증만으로 충분합니다.

1. [solapi.com](https://solapi.com) 가입 (휴대폰 본인인증).
2. **발신번호 관리**에서 대표님 휴대폰 번호를 등록합니다 (통신사 인증 절차, 수 분 소요).
3. **API Key 관리**에서 API Key와 Secret Key를 발급받습니다.
4. 콘솔에서 잔액을 소액(1만원 정도) 충전합니다 — 문자 1건당 약 20~30원입니다.
5. 위 "스크립트 속성"에 `SOLAPI_API_KEY`, `SOLAPI_API_SECRET`, `SMS_SENDER`(발신번호, 하이픈 없이 또는 있는 그대로 모두 가능)를 넣습니다.

---

## 3. 카카오 "나에게 보내기" (대표 아침 보고용, 약 10분, 선택)

이 단계를 건너뛰면 대표 보고는 자동으로 대표님 휴대폰 SMS로 대체 발송됩니다. 카톡으로 받고 싶으시면 아래를 진행하세요.

1. [developers.kakao.com](https://developers.kakao.com) 에서 애플리케이션을 하나 만듭니다.
2. **제품 설정 → 카카오 로그인**을 활성화하고, **동의 항목**에서 "카카오톡 메시지 전송"을 켭니다.
3. 아래 주소의 `{REST_API_KEY}`를 앱의 REST API 키로 바꿔 브라우저에서 접속 → 카카오 로그인 → 동의:
   `https://kauth.kakao.com/oauth/authorize?client_id={REST_API_KEY}&redirect_uri=https://localhost&response_type=code`
4. 리디렉션된 주소창의 `code=` 뒷부분 값을 복사합니다.
5. Apps Script 에디터에서 아래 임시 함수를 하나 만들어 실행하면 로그에 refresh_token이 출력됩니다(1회만 쓰고 지워도 됩니다):
   ```js
   function getKakaoToken(){
     const res = UrlFetchApp.fetch("https://kauth.kakao.com/oauth/token", {
       method:"post",
       payload:{grant_type:"authorization_code", client_id:"REST_API_KEY값", redirect_uri:"https://localhost", code:"위에서 복사한 code값"}
     });
     Logger.log(res.getContentText());
   }
   ```
6. 로그에 나온 `refresh_token` 값을 스크립트 속성 `KAKAO_REFRESH_TOKEN`에, REST API 키를 `KAKAO_CLIENT_ID`에 넣습니다.

---

## 4. GitHub Pages 배포 (약 10분)

이 컴퓨터에는 이미 GitHub CLI(`gh`)가 `sjbaik0431` 계정으로 로그인되어 있는 것을 확인했습니다. 아래 둘 중 하나를 알려주시면 됩니다.

- **A. 새 저장소를 만들어 달라** → 저장소 이름만 알려주시면 제가 `gh repo create`로 만들고, `index.html`을 올리고, Pages를 켜드리겠습니다.
- **B. 이미 쓰는 저장소가 있다** → 저장소 이름(`owner/repo`)을 알려주시면 그 안에 폴더를 만들어 올리겠습니다.

수동으로 직접 하시려면:
1. [github.com/new](https://github.com/new) 에서 저장소 생성 (Public이어야 무료 GitHub Pages 사용 가능).
2. 이 폴더의 `index.html`을 저장소 루트(또는 원하는 하위 폴더)에 업로드.
3. 저장소 **Settings → Pages**에서 Source를 "Deploy from a branch", 브랜치는 `main`(또는 `master`), 폴더는 `/(root)`로 설정 후 저장.
4. 몇 분 뒤 `https://{계정}.github.io/{저장소명}/index.html` 주소가 열립니다.
5. 주간 리포트 자동 커밋을 쓰려면 **Settings → Developer settings → Personal access tokens**에서 `repo` 권한의 토큰을 발급해 스크립트 속성 `GITHUB_TOKEN`에, 저장소 경로(`계정/저장소명`)를 `GITHUB_REPO`에, 공개 주소(`계정.github.io/저장소명`)를 `GITHUB_PAGES_HOST`에 넣습니다.

### 실시간 데이터 연결
대시보드 주소 뒤에 아래처럼 두 값을 붙이면 Google Sheets 데이터를 실시간으로 읽어옵니다.

```
https://{계정}.github.io/{저장소명}/index.html?api={1번에서 복사한 웹앱 URL}&token={WEBAPP_TOKEN에 넣은 값}
```

이 주소를 즐겨찾기해두고 매일 확인하시면 됩니다. 세대 정보 수정은 시트에서, 조회는 이 주소에서 하시고 "새로고침" 버튼으로 최신화하세요.

---

## 매달/매일 사람이 할 일 (자동화 이후)

- **은행 입금 대사**: 은행 홈페이지에서 거래내역 엑셀을 받아 "은행업로드" 시트에 붙여넣고, 시트 메뉴 "미건 관리 → 은행 입금 대사 실행"을 누릅니다. (주 1회 권장)
- **확인필요 입금 처리**: "입금" 시트에서 매칭상태가 "확인필요"인 행을 보고 어느 세대인지 수동으로 "매칭호실" 열에 적어 넣습니다.
- **수리 접수/처리**: "수리" 시트에 직접 입력하거나 상태를 바꿉니다.
- 그 외(청구 생성, 미납 판정, 문자 발송, 대표 보고, 주간 리포트)는 전부 자동입니다.
